import SectionHeader from "@/components/SectionHeader";

function Contact() {
    return (
        <div id="contact">
            <SectionHeader title="Contact" />
        </div>
    );
}

export default Contact;
